<div ng-controller="app.views.test.list as vm">
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <tr ng-repeat="test in tests">
                <td>{{test.name}}</td>
                <td>
                    <a class="glyphicon glyphicon-pencil" ui-sref="test.edit({test: test.id})"></a>
                    <a class="glyphicon glyphicon-remove" ng-click="delete(test)"></a>
                </td>
            </tr>
        </tbody>
    </table>
    <button type="button" class="btn btn-primary" ng-click="create()">Create new test</button>
    <script id="app.views.test.list.add.html" style="display: none" type="text/ng-template">
        <form ng-submit="ok()" name="form" class="form-horizontal">
            <div class="modal-header">
                <h3 class="modal-title">Create test</h3>
            </div>
            <div class="modal-body">
                <div class="form-group" ng-class="{'has-error': form.name.$dirty && form.name.$invalid, 'has-success': form.name.$dirty && !form.name.$invalid}">
                    <label for="test-add-name" class="control-label col-xs-2">Name:</label>
                    <div class="col-xs-10">
                        <input type="text" name="name" ng-model="model.name" id="test-add-name" class="form-control" required />
                        <span ng-show="form.name.$dirty && form.name.$error.required" class="help-block">Enter name.</span>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="submit" class="btn btn-primary" ng-disabled="form.$invalid">Create</button>
                <button type="button" class="btn btn-warning" ng-click="cancel()">Cancel</button>
            </div>
        </form>
    </script>

</div>
